public class ClashDetector{
    public static String checkClash(Wall wall,Pipe pipe){
        Point wallStart=wall.getStartPoint();
        Point wallEnd=wall.getEndPoint();
        Point pipeStart=pipe.getStartPoint();
        Point pipeEnd=pipe.getEndPoint();

        return null;
    }
}
